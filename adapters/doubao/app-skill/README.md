# 使用方法

1. 在豆包新建对话，选择内嵌的“应用生成”技能。
2. 上传完整的 `jennie-style-decoder-doubao-app.zip`。
3. 发送：`请完整读取压缩包，严格按 SKILL.md 和 APP_BUILD_SPEC.md，直接交付其中已完成的单页 HTML 应用；不要总结，不要缩减 52 套数据，不要重写选图逻辑。`
4. 豆包完成后，打开预览或下载生成的 HTML。

用户不需要单独上传 JSON、图片或代码。压缩包中的 HTML 已经内嵌完整数据和逻辑。
